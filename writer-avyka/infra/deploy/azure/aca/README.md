
# Azure Container Apps (ACA) deployment stub

This folder holds placeholders for deploying the API to ACA. Steps typically include:
1. Build and push image to GHCR or ACR.
2. `az containerapp create` with env vars and secret refs (WRITER_API_KEY, etc.).
3. Configure logging and scaling rules.

> Replace with your infra-as-code (Bicep/Terraform) as needed.
